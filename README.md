# Forecasting_system
A production-ready time-series forecasting pipeline built in Python. It automatically trains SARIMA, Prophet, XGBoost, and LSTM models, selects the best performer, and serves predictions via a FastAPI REST backend.
